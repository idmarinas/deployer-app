---
title: Configuration
description: The only initial configuration of {{project_name}} is the database: select an existing one or create a new one.
navigation:
  icon: i-tabler-settings
---

::note
:vars{n="project"} doesn't require any previous configuration to work. The only initial setup is the **database**: on first launch you can **select an existing database** or **create a new one**, and from there you just need to add your data (hosts, passkeys, etc.).
::